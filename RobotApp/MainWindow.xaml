<Window x:Class="RobotApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Robot" Height="300" Width="400" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" 
        Topmost="True" WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border CornerRadius="40" BorderThickness="3" Margin="20">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF121212" Offset="0"/>
                <GradientStop Color="#FF1A1A2E" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FF00F2FF" Offset="0"/>
                <GradientStop Color="#FF7000FF" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="#00F2FF" Opacity="0.4" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <!-- Eyes Container -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0">
                <!-- Left Eye -->
                <Grid Margin="25,0">
                    <Ellipse x:Name="LeftEyeOuter" Width="70" Height="70" Fill="#1000F2FF">
                        <Ellipse.Effect>
                            <BlurEffect Radius="20"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse x:Name="LeftEye" Width="50" Height="50" Fill="#FF00F2FF">
                        <Ellipse.RenderTransform>
                            <TranslateTransform x:Name="LeftEyeTransform"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>
                    <Ellipse Width="15" Height="15" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,-5,0,0" Opacity="0.6"/>
                </Grid>

                <!-- Right Eye -->
                <Grid Margin="25,0">
                    <Ellipse x:Name="RightEyeOuter" Width="70" Height="70" Fill="#1000F2FF">
                        <Ellipse.Effect>
                            <BlurEffect Radius="20"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse x:Name="RightEye" Width="50" Height="50" Fill="#FF00F2FF">
                        <Ellipse.RenderTransform>
                            <TranslateTransform x:Name="RightEyeTransform"/>
                        </Ellipse.RenderTransform>
                    </Ellipse>
                    <Ellipse Width="15" Height="15" Fill="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,-5,0,0" Opacity="0.6"/>
                </Grid>
            </StackPanel>

            <!-- Mouth -->
            <Grid VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,60">
                <Rectangle x:Name="Mouth" Width="140" Height="12" RadiusX="6" RadiusY="6" Fill="#FF00F2FF">
                    <Rectangle.Effect>
                        <DropShadowEffect BlurRadius="20" Color="#00F2FF" ShadowDepth="0" Opacity="0.9"/>
                    </Rectangle.Effect>
                </Rectangle>
            </Grid>
            
            <!-- Close Button -->
            <Button Content="×" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="20" Background="Transparent" Foreground="White" BorderThickness="0" FontSize="20" Cursor="Hand" Click="CloseButton_Click"/>
        </Grid>
    </Border>
</Window>
